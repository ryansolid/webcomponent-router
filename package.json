{
  "name": "webcomponent-router",
  "description": "A library for nested routing using webcomponents",
  "version": "0.2.0-beta.4",
  "author": "Ryan Carniato",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "coffee --compile --bare --no-header --output lib/ src/",
    "test": "npm run prepublish && mocha test/**/*.tests.coffee"
  },
  "devDependencies": {
    "mocha": "*",
    "coffeescript": "1.x"
  },
  "dependencies": {
    "eventemitter3": "2.x",
    "route-recognizer": "0.2.x",
    "document-register-element": "1.x"
  }
}
