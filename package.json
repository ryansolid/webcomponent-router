{
  "name": "webcomponent-router",
  "description": "A libary for nested client side routing using Webcomponents",
  "version": "0.1.0",
  "author": "Ryan Carniato",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "coffee --compile --bare --no-header --output lib/ src/",
    "test": "npm run prepublish && mocha test/**/*.tests.coffee"
  },
  "devDependencies": {
    "mocha": "*",
    "coffee-script": "*"
  },
  "dependencies": {
    "queue-async": "1.x",
    "eventemitter3": "*",
    "route-recognizer": "*",
    "document-register-element": "*"
  }
}
